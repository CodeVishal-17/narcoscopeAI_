# Core pipeline (processing, features, ML, hybrid) — required.
scikit-learn>=1.3
numpy>=1.24
joblib>=1.3

# Django REST backend (backend/) — wraps the pipeline with a DB + API.
django>=5.0
djangorestframework>=3.15
django-cors-headers>=4.4

# Telegram ingestion — REAL, official MTProto API. Required for live data.
telethon>=1.34
python-dotenv>=1.0

# --- Optional extras (install only what you use) ---

# LLM stage (pass 3) — Claude adjudication of ambiguous messages.
#   pip install anthropic
# anthropic>=0.40

# Language handling (optional, improves Hinglish / lang detection):
#   pip install langdetect
# langdetect>=1.0.9

# Instagram scraping (unofficial, ToS-risky, not wired into the backend yet):
#   pip install instaloader
# instaloader>=4.10
